# As-Sabiquun `/about` — live August Health 1:1 comparison bar

Primary rendered reference: https://www.augusthealth.com/about (captured live on 30 August 2026)

For `/about`, these measured live mechanisms override landing-only composition rules in `design-system.md`. As-Sabiquun keeps its own logo, palette values, truthful copy, four service routes, project photography, footer links, and Islamic-service meaning; August names, portraits, press marks, claims, and copy must never appear.

1. **The floating navigation and announcement behave exactly like the live reference.** Desktop uses a centered pill about `calc(100% - 128px)` wide, `66px` high, and `16px` from the viewport top; it remains sticky through the page. Its surface is lavender over indigo and near-white over cream/white. A separate centered announcement pill sits immediately below the navigation in the opening only. At `390px`, the nav is `335px` wide, about `50px` high, `20px` from the left, and contains the brand plus one `44px` menu control; the announcement pill remains visible below it without touching the hero headline.

2. **The opening matches the live indigo viewport, not merely its mood.** At desktop the indigo field has a `720px` floor, the centered serif thesis is `48/52.8px` and roughly `630px` wide, and the support line is a centered two-line sans block below it. Six or more bright one-pixel coral, pink, blue, and green circles are cropped by every viewport edge; their visible arcs are large and high-contrast, while none crosses the text. At `390px`, the thesis is exactly `32/35.2px` on a `335px` rail, the body becomes `18/28.8px`, and the first blue mission panel begins in the same opening scroll without a detached spacer.

3. **The mission is a tall blue document panel embedded in the indigo field.** Desktop centers one approximately `750px`-wide rounded blue panel. Its `48/52.8px` mission heading, one-line lead, and long-form paragraphs use the same alignment and generous cadence as the live page; the text column inside is about `700px`, and the panel occupies roughly `740px` before its lower edge. At mobile the panel is `335px` wide with `24px` radii and `24px` side insets; the heading is `32/35.2px`, body copy is left-aligned and vertically separated, and the panel runs continuously into the shallow concave cream transition.

4. **The operating story copies the live origin spread geometry.** After the concave transition, a cream section uses the full `1160px` rail and at least `760px` of vertical clearance. Desktop places the outlined capsule and `48/52.8px` thesis in a left column about `600px` wide, with two `120–132px` circular project photographs directly beneath; the right column begins near the same heading baseline and uses `16/25.6px` paragraphs, attribution, then one blue pill. Mobile stacks left thesis and images before the narrative, keeps the heading at `32/35.2px`, and preserves large breathing room before the next section.

5. **The service platform uses the live compact three-column grid.** A white section centers a capsule, `48/52.8px` heading, and two-line support on the `1160px` rail. Desktop cards are a strict `3 × 2` grid: each cell is about `384 × 207px`, `24px` radius, with only `4–8px` seams, a `32px` serif title, colored line symbol, `48px` white arrow circle, and short body copy. As-Sabiquun's four real services occupy the first four cells in authoritative order; the remaining two cells are intentionally absent rather than fabricated, leaving clean white grid space. At mobile, cards become a single `335px` column, `157–208px` tall, with near-touching seams and unchanged `16/25.6px` body type.

6. **Amanah principles inherit the live news-list structure one-for-one.** The section begins on white with a centered `48/52.8px` heading and a quiet top rule. Each desktop principle row is at least `165px` tall and uses four fixed zones: small numeral at far left, `30–32px` serif title, restrained `16px` explanation beneath or beside it, and a `48px` circular arrow-equivalent line mark at far right; only one-pixel rules separate rows and there are no card surfaces. Mobile rows stack numeral, title, explanation, and end mark on the `335px` rail with the same generous vertical whitespace as the live news list.

7. **The page closes cream-to-green exactly like the live site.** Replace the blue closing band with a cream editorial field: desktop uses roughly `500–560px` of centered whitespace, a `48/52.8px` serif statement, one `18px` support line, and one blue pill; mobile uses `32/35.2px` type and a compact cream field while retaining a `48px+` action. The cream field meets the existing deep-green footer directly with no curve, blue seam, blank gap, overlay, or sticky reveal. Reduced motion freezes circles and hover transforms; horizontal overflow is always zero.
