# As-Sabiquun `/about` — August Health rendered comparison bar

Primary reference: https://www.augusthealth.com/about

The page must preserve As-Sabiquun's truthful mission, existing service routes, original project imagery, approved-partner workflow, reviewed-evidence promise, navigation, and footer. August Health's copy, people, portraits, product marks, press, customers, metrics, and credentials must not appear.

1. **The opening viewport is one quiet indigo statement:** At `1440x900`, the floating `1160x66px` navigation sits about `16px` from the top on a translucent lavender shell. A centered serif headline occupies no more than roughly `760px` and uses about `56-64px` type with a short `18-20px` support line below. Four or five single-pixel circles, each roughly `360-520px` across, are cropped by the viewport perimeter; they never cross the headline. At least `40%` of the frame remains undecorated indigo space.

2. **Hero and mission read as one continuous environment:** The indigo opening continues into a second full-width mission stage rather than cutting to an unrelated card grid. A saturated blue panel roughly `750px` wide centers a `48/53px` serif mission statement and a narrow lead, then presents three short left-aligned paragraphs with `48-72px` vertical separation. Peripheral outline circles remain behind the panel, and a shallow concave cream edge closes the dark environment.

3. **The operating story is a sparse two-column editorial spread:** On the `1160px` rail, a small outlined capsule sits above two approximately equal columns. The left column uses a `48/53px` serif thesis and two `120-132px` circular project-image crops; the right column uses `16/25.6px` paragraphs, a restrained attribution line, and one blue pill. The section is at least `760px` tall on desktop and contains no cards, statistics, founder identities, or decorative dashboard art.

4. **What-we-do uses equal, low-contrast platform tiles:** A centered capsule, `48/53px` heading, and two-line support introduce four equal tiles on the `1160px` rail. The tiles form a regular `2x2` grid with `16px` gaps, `20-24px` radii, pale cream/pink/green/lavender surfaces, a `30-34px` serif title, one short body paragraph, and one `48px` circular arrow. Tiles do not lift; only the arrow or icon shifts on hover.

5. **Principles inherit the reference's editorial list rhythm:** The principles section starts with a centered `48/53px` heading and substantial top clearance. Amanah, Clarity, and Verification appear as three full-width rows separated by quiet one-pixel rules, each at least `150px` tall. A small numeral or line icon occupies the left utility column, the principle title is roughly `30-34px` serif, and the explanation remains subordinate; there are no badges, dates, fake links, or heavy card containers.

6. **The closing is one decisive transition, then the existing footer:** A full-width action-blue band is at least `540px` tall, contains one centered serif statement, one short support line, and one light pill action with wide surrounding whitespace. It flows directly into the existing deep-green footer with no sticky reveal, overlap, invented partner marks, or detached white gap. The brand palette may differ from the cream August closing, but its scale, simplicity, and single-action hierarchy must match.

7. **Mobile preserves the same hierarchy without shrinking it into a desktop canvas:** At `390px`, navigation collapses to the existing `44px` menu control, the opening headline is `40-44px` with circles reduced and cropped outside the text column, and the mission panel becomes full-bleed or near-full-bleed without horizontal scroll. The operating-story columns stack heading/images before narrative, platform tiles become one column, and principle rows keep `44px` targets and readable `16px` body type. All motion resolves to a static final state under `prefers-reduced-motion`.
