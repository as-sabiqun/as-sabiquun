# As-Sabiquun landing page — comparison bar

Reference: https://www.augusthealth.com/ (captured 2026-08-28)

This is a **concept-demo** landing page. Any trust-strip marks are original fictional marks and must not be presented as real partners, endorsements, or accreditations.

Desktop measurements below use a **1280 × 720 viewport**. The page must preserve the same proportional rhythm at smaller widths rather than merely containing the same sections.

1. **Floating navigation:** The white capsule begins at `top: 17px`, is `64px` high and approximately `1120px` wide. It remains fixed while every content band passes behind it. Its brand sits at the far left, the primary links occupy one centered row, and one filled action pill touches the right inset. At 1280px the As-Sabiquun nav must be within ±3px of those bounds.
2. **Rotating circular hero:** The opening `h1` begins at document `y: 209px`, is `560px` wide and `128px` tall, followed by a two-line body and one primary action. The visual field is predominantly paper/white in the centre; cropped circular interface and human fragments enter from all four edges. The background is an oversized `border-radius: 1600px` ellipse that rotates and scales from page `y: 0–620px`: its broad curved lower chord at rest becomes a flat horizontal edge by the trust rail. Reduced-motion mode may hold the final flat state.
3. **Trust rail:** The trust heading begins at `y: 800px`, is `35px` tall, and sits above one horizontal row of six detailed monochrome original fictional organisation marks. The platform eyebrow appears near `y: 1160px`. The rail has no cards, borders, or CTA; the concept-status disclosure stays compact and secondary.
4. **Six-card platform grid:** The platform heading begins at `y: 1213px`, is a **single line**, `560px` wide and `53px` tall. The first card row begins around `y: 1327px`; card titles begin at `y: 1361px`. Six cards form three vertical rows of two; row-title baselines repeat at `y: 1361px`, `1941px`, and `2521px` (580px rhythm). Each card has a pale tint, 30px radius, title/copy at top, large original graphic below, and a 48px circular arrow at top right. On hover the platform card itself stays stationary with no new shadow; only the arrow translates `9–10px` right. At a 390px viewport the hero H1 begins around `y: 200px` at `40px`, and the platform heading begins around document `y: 1059px`.
5. **Values band:** The dark band begins at `y: 3244px` and is `1231px` tall. Its lead headline begins at `y: 3437px`, is `740px` wide and `106px` tall. A single `832px × 588px` route frame begins at `x: 216px / y: 3695px`, has a 48px radius and 2px low-contrast border. Inside it are three `764px × 152px` cards at `x: 250px`, `y: 3729/3913/4097px`, 16px radius, with green/gold/coral-pink As-Sabiquun tones retained on mobile. Each card uses a balanced two-column interior: a large serif principle occupying ~48% at left and a smaller explanatory paragraph occupying ~48% at right; no centered single-statement layout. The eyebrow is an outlined capsule. The transition is full-width and flat; any circular entrance overlay must resolve before the headline reaches the viewport.
6. **Case study + story rail:** The cream case-study band begins at `y: 4475px` and is `2001px` tall; its headline begins at `y: 4668px`, is `1080px` wide and `106px` tall. The full case-video component is exactly `960px × 748px` at `x: 152px / y: 4894px`, with a 16px radius; its lower 210px is a white quote/caption panel. The story rail begins at `y: 5770px`. At the 1280px viewport it contains `376px × 450px` solid-colour editorial cards plus slivers of neighbors at both edges, separated by ~30px gaps. Card title content is 312px wide; stat and title start at `y: 5802/5882px`. Cards use typography/graphic marks rather than full-bleed photos, and the bottom copy is divided by one hairline. Two 48px outlined circular arrow controls sit centered below on a faint dot-grid field. Hover translates the arrow about 10px right (the live platform card measured `9.6px`).
7. **Pinned closing transition and footer:** The dark closing section begins at `y: 6476px`, is one viewport (`720px`) tall, and its 688px-wide headline begins at `y: 6718px` with a 158px height. The headline is exactly three lines at `48px / 52.8px`, followed only by one 46px CTA at `y: 6908px`; there is no eyebrow or supporting paragraph in this band. The footer is `829px` tall with `position: sticky; bottom: 0`, revealing behind the main content. It is a full-bleed two-tone split: roughly 41% brand panel with an oversized circular seal and 59% deeper panel with two tiers of large multi-column navigation. On mobile the footer returns to normal flow, omits the desktop seal panel, and exposes a compact two-column link grid immediately after the closing band. The original floating navigation remains the only header. Target desktop document height is `8025px ± 120px`.
