# Task Plan: Action-first dashboards

> Source: `docs/flows/dashboard-experience/spec.md`

1. Reorder the vendor overview so offers and recent work appear before charts.
2. Use the accepted assignment timestamp for the vendor trend.
3. Reorder the admin overview and job register so the action queue is encountered before analytics.
4. Simplify the primary customer, vendor, and admin page hooks.
5. Keep existing accessible SVG charts and exact-data tables; add no chart dependency.
6. Run lifecycle tests, lint, typecheck, and the production build.
7. Push the verified change, then review deployed screenshots with the user.

## Checkpoint

The user approved implementation and requested a push followed by a collaborative visual review.
